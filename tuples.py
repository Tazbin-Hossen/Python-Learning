#tuples uses foe the storing group of values but the main difference of list and tuples are " list are (homogeneous and mutable) and tuples are (heterogeneous and immutable) "

tup = (1,2)
print(tup[0])

